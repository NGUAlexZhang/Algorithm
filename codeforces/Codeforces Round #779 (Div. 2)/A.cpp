#include <bits/stdc++.h>
using namespace std;
char s[2005];
int main(){
    int T;
    cin>>T;
    while(T--){
        int len;
        cin>>len;
        cin>>(s+1);
        int ans=0;
        for(int i=1;i<=len;i++){
            if()
        }
    }
    return 0;
}